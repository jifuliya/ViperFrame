apply plugin: 'java'

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    compile project(path: ':portalannotation')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
